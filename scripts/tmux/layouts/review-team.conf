# ============================================================
# review-team.conf - レビューチームレイアウト（3名）
# ============================================================
# メイン 60% + サイド 4ペイン (2x2)
#
# +----------------------------------+
# |                       |DevT|Agent|
# |    Claude Code        | 20%| 50% |
# |       60%             |----|-----|
# |                       |MCP |Git+ |
# |                       |Hlth|Rsrc |
# +----------------------------------+
#
# 形式: PANE_NAME SPLIT_DIR SPLIT_PCT SCRIPT_NAME ARGS
# ============================================================

# 右側にDevTools監視（全幅の40%）
devtools h 40 devtools-monitor.sh __PORT__

# 右側の右半分にAgent Teams監視
agent-teams h 50 agent-teams-monitor.sh

# 右上 DevTools の下にMCPヘルス
mcp-health v 50 mcp-health-monitor.sh

# 右下 Agent の下にGit+リソース
git-resource v 50 git-status-monitor.sh __PROJECT__
