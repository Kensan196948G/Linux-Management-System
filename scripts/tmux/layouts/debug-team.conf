# ============================================================
# debug-team.conf - デバッグチームレイアウト（3名）
# ============================================================
# メイン 70% + サイド 3ペイン
#
# +----------------------------------+
# |                       |DevT|Agent|
# |    Claude Code        |+MCP| 50% |
# |       70%             |----|-----|
# |                       | Log Tail |
# |                       |   100%   |
# +----------------------------------+
#
# 形式: PANE_NAME SPLIT_DIR SPLIT_PCT SCRIPT_NAME ARGS
# ============================================================

# 右側にDevTools+MCP監視（全幅の30%）
devtools h 30 devtools-monitor.sh __PORT__

# 右側の右半分にAgent Teams監視
agent-teams h 50 agent-teams-monitor.sh

# 右側下部にリソース監視（全サイドの50%）
resource v 50 resource-monitor.sh
