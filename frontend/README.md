# Frontend - WebUI

**HTML/CSS/JavaScript ベースの Linux 管理 WebUI**

---

## 📋 概要

セキュリティファースト設計の WebUI。開発環境と本番環境で UI を分離。

---

## 📂 構成

```
frontend/
├── dev/
│   └── index.html           # 開発環境用 UI（デモデータ有り）
│
├── prod/
│   └── index.html           # 本番環境用 UI（デモデータ無し）
│
├── css/
│   └── style.css            # 共通スタイルシート
│
└── js/
    ├── api.js               # API クライアント
    ├── components.js        # UI コンポーネント
    └── app.js               # アプリケーションロジック
```

---

## 🌐 環境別 UI

### 開発環境（frontend/dev/）

- **URL**: http://localhost:3000/dev/
- **特徴**:
  - 【開発】バッジ表示
  - デモアカウント情報表示
  - 全サービス操作可能（デモ）
  - デバッグ情報表示

### 本番環境（frontend/prod/）

- **URL**: https://localhost:8443/prod/
- **特徴**:
  - 【本番】バッジ表示
  - デモアカウント情報非表示
  - 許可されたサービスのみ表示
  - デバッグ情報非表示

---

## 🚀 使い方

### 開発環境でのアクセス

```bash
# 1. バックエンドサーバーを起動
./scripts/start-dev-server.sh

# 2. ブラウザでアクセス
http://localhost:3000/dev/index.html
```

### デモアカウント（開発環境のみ）

| Email | Password | Role |
|-------|----------|------|
| viewer@example.com | viewer123 | Viewer（閲覧のみ） |
| operator@example.com | operator123 | Operator（操作可能） |
| admin@example.com | admin123 | Admin（全権限） |

---

## 🎨 機能

### 1. ログイン
- JWT トークンベースの認証
- LocalStorage にトークン保存
- 自動ログアウト（トークン期限切れ）

### 2. ダッシュボード
- CPU使用率（リアルタイム）
- メモリ使用率
- ディスク使用状況
- システム稼働時間

### 3. サービス操作
- 許可されたサービスのみ表示
- 確認ダイアログ表示
- 操作結果の表示

### 4. ログ閲覧
- サービス選択（ドロップダウン）
- 取得行数指定（1-1000）
- ログビューア（モノスペースフォント）

---

## 🛡️ セキュリティ実装

### クライアント側検証

```javascript
// 入力検証（サーバー側でも再検証）
const forbiddenChars = /[;|&$()` ><*?{}[\]]/;
if (forbiddenChars.test(input)) {
    alert('禁止文字が含まれています');
    return false;
}
```

### 選択式 UI 優先

```html
<!-- ✅ 推奨: ドロップダウン -->
<select id="service-name">
    <option value="nginx">nginx</option>
    <option value="postgresql">postgresql</option>
</select>

<!-- ❌ 非推奨: テキスト入力 -->
<input type="text" id="service-name">
```

### JWT トークン管理

- LocalStorage に保存
- 全 API リクエストに自動付与
- 401 エラー時に自動ログアウト

---

## 🧪 テスト

### Playwright E2E テスト（作成予定）

```javascript
// tests/e2e/test-ui.spec.js
test('ログインからサービス再起動まで', async ({ page }) => {
    await page.goto('http://localhost:3000/dev/');
    await page.fill('#email', 'operator@example.com');
    await page.fill('#password', 'operator123');
    await page.click('button[type="submit"]');

    // ダッシュボードが表示されることを確認
    await expect(page.locator('#dashboard-section')).toBeVisible();
});
```

---

## 📚 参考資料

- [../backend/README.md](../backend/README.md) - バックエンド API
- [../CLAUDE.md](../CLAUDE.md) - セキュリティ原則
- [../config/dev.json](../config/dev.json) - 開発環境設定

---

**最終更新**: 2026-02-05
